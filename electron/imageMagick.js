import gm from 'gm'

export function imageTransform(path){
    console.log("imageTransform path", path);
    // path.map((x)=>{
    //     gm(x).resize(100, 100).write('output.jpg', function (err) {
    //       if (err) console.log(err)
    //     })
    // })
}
